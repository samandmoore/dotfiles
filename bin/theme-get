#!/usr/bin/env bash

SED="sed"
if [[ $(uname) == "Darwin" ]]; then
  SED="gsed"
fi

basename "$(realpath "$HOME/.config/dotfiles/current/theme" 2>/dev/null)" | $SED -E 's/(^|-)([a-z])/\1\u\2/g; s/-/ /g'
