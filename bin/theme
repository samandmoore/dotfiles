#!/usr/bin/env bash
set -euo pipefail

DOTFILES=~/.dotfiles
THEME=$1

ln -sf \
  "../../etc/themes/$THEME/alacritty.toml" \
  "$DOTFILES/config/alacritty/theme.toml"

# TODO
# configure tmux theme
# configure neovim theme
# configure vscode theme
# configure gnome theme
