#!/usr/bin/env bash

package_name=$(paru -Slq | fzf --multi --preview 'paru -Sii {1}' --preview-window=down:75%)

if [[ -n "$package_name" ]]; then
  paru -S --noconfirm "$package_name"
  sudo updatedb
fi
